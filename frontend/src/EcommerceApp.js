import { LoginRouter } from "./routers/LoginRouter";

export const EcommerceApp = () => {
  return (
      <LoginRouter />
  );
};

//rutas login + > despues de login ( home ) > (403 si no encuenta pagina)
